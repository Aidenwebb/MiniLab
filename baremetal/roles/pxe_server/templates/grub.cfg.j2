set timeout=1

menuentry '{{ boot_image_iso_url | basename | splitext | first }} (PXE)' {
    linux vmlinuz \
        ip=dhcp \
        inst.ks=http://{{ ansible_default_ipv4.address }}/init-config/${net_default_mac}.ks
    initrd initrd.img
}